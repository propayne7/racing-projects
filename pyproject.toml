[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "telemetry-tool"
version = "0.1.0"
description = "Motorsport telemetry analysis and translation tool"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
